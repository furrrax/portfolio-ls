.nav__list {
    display: flex;
    justify-content: center;
}

.nav__list-item {
    font-family: 'Open Sans', sans-serif;
    font-weight: 400;
    color: #fff;
    font-size: 16px;
    margin-right: 60px;
    &:last-child {
        margin-right: 0;
    }
}